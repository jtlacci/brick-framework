# Template module contract

This folder is boilerplate, not a real domain implementation.

- Copy this entire folder and rename `example_module` everywhere it appears.
- Keep the `input/`, `runner/`, and `src/` contracts in the copied module.
- Keep the top-level `__init__.py` limited to exporting `run`.
- Replace stubs and TODOs only when building an actual module.
